@page
@model Agriloco.Api.Pages.Map.FarmMapModel

<h2>Farm Map Viewer (Disabled)</h2>

<p>
    This page was built for the old MapCell DTO shape (FarmName, ColorCode, CenterLat/Lng, FeatureName).
</p>

<p>
    We are now using a simplified grid model and an automatic palette system.
    A new map/grid viewer will be built next using:
    <code>/api/Map/cells</code> and <code>/api/Palette</code>.
</p>