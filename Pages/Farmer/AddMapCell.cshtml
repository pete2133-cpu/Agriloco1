@page
@model Agriloco.Api.Pages.Farmer.AddMapCellModel

<h2>Add Map Cell (Disabled)</h2>

<p>
    This page is disabled while we transition to the new workflow:
</p>

<ol>
    <li>Create crop assets</li>
    <li>Manage crop assets (availability + details)</li>
    <li>Palette is computed automatically</li>
    <li>Paint tiles on a grid using a new grid painter page (next step)</li>
</ol>

<p>
    Next we will build the new grid painter page that uses:
    <code>/api/Palette</code> and <code>/api/Map/cells</code>.
</p>